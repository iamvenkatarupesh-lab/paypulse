# WARNING: This is a LOCAL DEVELOPMENT secret only.
# Real production secrets should NEVER live in Git.
# In Phase 9 (AWS), we'll use AWS Secrets Manager + External Secrets Operator.
apiVersion: v1
kind: Secret
metadata:
  name: paypulse-postgres-secret
  namespace: paypulse
  labels:
    app: paypulse-postgres
type: Opaque
stringData:
  POSTGRES_PASSWORD: "paypulse_dev_password_change_me"
