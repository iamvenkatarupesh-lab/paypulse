apiVersion: v1
kind: Service
metadata:
  name: paypulse-backend
  namespace: paypulse
  labels:
    app: paypulse-backend
spec:
  type: ClusterIP
  selector:
    app: paypulse-backend
  ports:
    - name: http
      port: 3001
      targetPort: http
      protocol: TCP
